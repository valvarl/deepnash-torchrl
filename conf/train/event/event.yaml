# @package train

defaults:
  - _self_

event_config:
  wandb: true
  log_interval: 10
  eval_interval: 20
  checkpoint_interval: 50
  sync_intervals:
    intra_instance: 5
    inter_instance: 20
  actor_update_interval: 5