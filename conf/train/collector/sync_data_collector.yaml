# @package train

collector_config:
  collector_type: "sync"
  n_workers: 1
  frames_per_batch: 3600
  max_frames_per_traj: 3600
  total_frames: -1
  policy_device: "cuda"
  env_device: "cpu"
  storing_device: "cuda"
  split_trajs: true
  reset_at_each_iter: false
